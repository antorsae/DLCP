[pytest]
testpaths = tests
markers =
    slow: long-running simulation test
    gpsim: requires gpsim binary
    wire: gpsim wire/current-loop chain test
    hardware: live hardware test requiring the DLCP rig, camera, and IR emitter
addopts = -ra
