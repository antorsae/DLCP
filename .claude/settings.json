{
  "$schema": "https://claude.ai/settings/schema/v1",
  "_comment": "Project-scoped Claude Code settings for the DLCP firmware analysis repo.  Checked into git so every contributor / future Claude Code session picks up the same hooks.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/codex-review-on-commit.sh"
          }
        ]
      }
    ]
  }
}
