//! `dlcp-sim` replay CLI.
//!
//! Phase 5 deliverable per `docs/SIM_REWRITE_RUST_SPEC.md` §9.  Reads
//! a `case.json` describing an initial chain state + stimulus stream,
//! runs the simulator, and optionally writes a trace.  Stub
//! implementation; the body lands in P5.3.

fn main() {
    eprintln!(
        "dlcp-sim CLI: P5.3 stub.  Replay format + main loop land in \
         a follow-up commit."
    );
    std::process::exit(2);
}
